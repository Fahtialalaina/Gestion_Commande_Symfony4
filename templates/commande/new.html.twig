{% extends 'base.html.twig' %}

{% block title %}Commande | Gestion de Commande{% endblock %}

{% block entete %}
    <!-- ============================================================== -->
    <!-- Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
             <div class="page-breadcrumb">
                <div class="row">
                    <div class="col-12 d-flex no-block align-items-center">
                        <h4 class="page-title">Commande</h4>
                        <div class="ml-auto text-right">
                            <nav aria-label="breadcrumb">
                                <ol class="breadcrumb">
                                    <li class="breadcrumb-item"><a href="{{ path('commande_index') }}">Commande</a></li>
                                    <li class="breadcrumb-item active" aria-current="page"><a href="{{ path('home') }}">Accueil</a></li>
                                </ol>
                            </nav>
                        </div>
                    </div>
                </div>
            </div>
    <!-- ============================================================== -->
    <!-- End Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
{% endblock %}

{% block body %}
    <h1>Création d'une nouvelle Commande</h1>

{# {{ form_start(form) }}
    {{ form_row(form.numc) }}
    {{ form_row(form.dateComm) }}
    {{ form_row(form.client) }}

    <h3>Ligne Commande</h3>
    <ul class="lcommande">
        {% for lcom in form.LCommande %}
            <li>{{ form_row(lcom.produit) }}</li>
            <li>{{ form_row(lcom.qte) }}</li>
        {% endfor %}
    </ul>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}

    <form action="" method="post">


        <div class="row">
            <div class="col-6">
                <div class="card alert alert-dark">
                    <div class="card-body">
                        <h4 class="card-title">Informations sur le Client</h4>
                        <hr/>
                        <div class="row">
                            <div class="col-12" style="margin-bottom: 20px;">N°Commande : {{ numc }}</div>
                            <div class="col-12" style="margin-bottom: 20px;">Date du commande : {{ form_widget(form.dateComm) }}</div>
                            <div class="col-12" style="margin-bottom: 20px;">Client : {{ form_widget(form.client) }}</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

                    <table class="table">
                        <th class="col-md-1">Produit</th>
                        <th class="col-md-1">Quantité</th>
                        <th class="col-md-1">Action</th>
                        <tr>
                            <td class="col-md-1">{{ form_widget(f.produit) }}</td>
                            <td class="col-md-1">{{ form_widget(f.qte) }}</td>
                            <td class="col-md-1">
                                <input type="submit" value="Add" name="bt" class="btn btn-xarning"/>
                                <i class="glyphicon glyphicon-save"></i>
                            </td>
                        </tr>
                    </table>
                    <table class="table table-condensed">
                        <thead class="thead-dark">
                            <tr>
                                <th>Ligne</th>
                                <th class="col-md-2">Désignation Article</th>
                                <th class="col-md-1">Quantité</th>
                                <th class="col-md-1">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for t in lcomm %}
                            <tr>
                                <td class="col-md-2">{{ t.lig }}</td>
                                <td class="col-md-1">{{ t.produit.libelle }}</td>
                                <td class="col-md-1">{{ t.qte }}</td>
                                <td class="col-md-1">
                                    <a href="{{ path('supprimer', {'id': t.lig}) }}" 
                                    class="btn btn-danger">
                                    <i class="glyphicon glyphicon-apple"></i>
                                    Supprimer
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
            <td>
                <input type="submit" value="Valider" name="bt" class="btn btn-success"/>
            </td>
    </form>

    <button class="btn" style="position: relative; margin-left: 75px; margin-top: -58px; padding: 7px 15px;"><a href="{{ path('commande_index') }}" style="color: #343a40;">Retour</a></button>
{% endblock %}
