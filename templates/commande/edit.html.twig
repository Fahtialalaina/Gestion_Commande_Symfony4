{% extends 'base.html.twig' %}

{% block title %}Commande | Gestion de Commande{% endblock %}

{% block entete %}
    <!-- ============================================================== -->
    <!-- Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
             <div class="page-breadcrumb">
                <div class="row">
                    <div class="col-12 d-flex no-block align-items-center">
                        <h4 class="page-title">Commande</h4>
                        <div class="ml-auto text-right">
                            <nav aria-label="breadcrumb">
                                <ol class="breadcrumb">
                                    <li class="breadcrumb-item"><a href="{{ path('commande_index') }}">Commande</a></li>
                                    <li class="breadcrumb-item active" aria-current="page"><a href="{{ path('home') }}">Accueil</a></li>
                                </ol>
                            </nav>
                        </div>
                    </div>
                </div>
            </div>
    <!-- ============================================================== -->
    <!-- End Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
{% endblock %}

{% block body %}
    <h1>Modification de la Commande</h1>

    {{ include('commande/_form.html.twig', {'button_label': 'Modifier'}) }}

    <button class="btn" style="position: relative; margin-left: 85px; margin-top: -58px; padding: 6px 15px;"><a href="{{ path('commande_index') }}" style="color: #343a40;">Retour</a></button>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
